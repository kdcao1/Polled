services:
  polled-web:
    container_name: polled
    build:
      context: .
      dockerfile: Dockerfile
      args:
        EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID: ${EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID}
        EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID: ${EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID}
        EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID: ${EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID}
        EXPO_PUBLIC_ANALYTICS_INGEST_URL: ${EXPO_PUBLIC_ANALYTICS_INGEST_URL:-https://analytics.polled.app/analytics}
        EXPO_PUBLIC_MAINTENANCE_STATUS_URL: ${EXPO_PUBLIC_MAINTENANCE_STATUS_URL:-https://admin.polled.app/api/public/maintenance}
    restart: unless-stopped

  notification-worker:
    container_name: polled-notification-worker
    build:
      context: ./server/notification-worker
    env_file:
      - ./server/notification-worker/.env
    environment:
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:-polled-f5b29}
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/firebase-service-account.json
      NOTIFICATION_WORKER_POLL_MS: ${NOTIFICATION_WORKER_POLL_MS:-5000}
      NOTIFICATION_WORKER_BATCH_SIZE: ${NOTIFICATION_WORKER_BATCH_SIZE:-10}
      ANALYTICS_HTTP_PORT: ${ANALYTICS_HTTP_PORT:-8787}
      ANALYTICS_SQLITE_PATH: /app/data/analytics.sqlite
    secrets:
      - source: firebase-service-account
        target: firebase-service-account.json
    volumes:
      - ./server/notification-worker/data:/app/data
    restart: unless-stopped

  admin-dashboard:
    container_name: polled-admin-dashboard
    build:
      context: ./admin-dashboard
    env_file:
      - ./admin-dashboard/.env
    environment:
      GOOGLE_SERVICE_ACCOUNT_KEY: ""
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/firebase-service-account.json
      ADMIN_SECRET: ${ADMIN_SECRET:?Set ADMIN_SECRET in the root .env before starting the admin dashboard}
      ANALYTICS_SQLITE_PATH: /analytics/analytics.sqlite
    secrets:
      - source: firebase-service-account
        target: firebase-service-account.json
    volumes:
      - ./server/notification-worker/data:/analytics:ro
    depends_on:
      - notification-worker
    restart: unless-stopped
    
networks:
  default:
    external: true
    name: serverout

secrets:
  firebase-service-account:
    file: ${GOOGLE_APPLICATION_CREDENTIALS:?Set GOOGLE_APPLICATION_CREDENTIALS to your Firebase service account JSON path}
